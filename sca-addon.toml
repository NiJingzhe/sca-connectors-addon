[addon]
name = "connverify"
version = "0.4.1"
license = "Apache-2.0"
skill_path = "skill"

[compat]
sca = ">=2.1,<3"

[runtime]
kind = "python-env"
platforms = ["macos-arm64", "macos-x86_64", "linux-x86_64", "linux-aarch64"]
check_cmd = "python3 -c 'import connverify, gmsh, numpy, simplecadapi' 2>/dev/null || python -c 'import connverify, gmsh, numpy, simplecadapi'"
